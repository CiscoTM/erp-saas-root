CREATE TABLE IF NOT EXISTS menu_template_ref (
                                                 id BIGINT PRIMARY KEY,
                                                 name VARCHAR(255) NOT NULL
    );
